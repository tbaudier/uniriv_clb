# uniriv_clb
Python tools for Uniriv we use at the CLB  

pip install -v -e .  
anonymize.py -i <patient_sub_folder_path> -p <new_patient_id> -id <new_patient_id>  
sort_dcm_files.py -i <input_folder> -o <output_folder>  


